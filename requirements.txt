# Pinned to the versions used for the paper's experiments (Python 3.11)
torch==2.6.0
numpy==2.2.6
scipy==1.17.1
scikit-learn==1.8.0
pandas==3.0.3

# Convex inner layer (G via CVXPYLayer) and the dual-price LPs
cvxpy>=1.5
cvxpylayers>=0.1.6
diffcp>=1.0.23

# Dataset downloads (UCI) and figures
ucimlrepo>=0.0.7
matplotlib>=3.9

# Tests
pytest>=8.0
